import { Injectable } from '@nestjs/common';

@Injectable()
export class AuthService {
  signUpLocal() {
    // return this.usersService.getUsers();
  }

  signInLocal() {
    // return this.usersService.getUsers();
  }

  refreshToken() {
    // return this.usersService.getUsers();
  }

  logout() {
    // return this.usersService.getUsers();
  }
}
